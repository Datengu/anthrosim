name: Apply AV6-006 local M9 tie repair

on:
  push:
    branches: ["audit/repair-711-av6-006"]
    paths: [".github/workflows/apply-av6-006-local-tie-repair.yml"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0
      - name: Apply decision-local M9 coupling repair
        run: |
          python3 - <<'PY'
          from pathlib import Path

          travel = Path('crates/anthrosim-core/src/temporary_travel.rs')
          text = travel.read_text()
          start = text.index('/// All grid-reflection frames that realize')
          end = text.index('/// Versioned M9.4 route-cost assumptions.')
          replacement = r'''/// All grid-reflection frames that realize the lexicographically minimal declared state of
/// one origin's reachable traversable component. The component is normalized to its own bounding
/// box so causally isolated world padding cannot relabel a local M9 tie. More than one retained
/// reflection means the reachable component itself has an exact automorphism; destinations related
/// inside that automorphism remain one exchangeable class.
#[derive(Debug, Clone, PartialEq, Eq)]
pub(crate) struct TemporaryDestinationCanonicalFrame {
    min_x: u32,
    min_y: u32,
    width: u32,
    height: u32,
    reflections: Vec<GridReflection>,
}

pub(crate) fn destination_canonical_frame(
    world: &World,
    region: &FocalRegion,
    context: TemporaryDestinationCouplingContext,
    reachable: &[bool],
    origin: CellId,
) -> Option<TemporaryDestinationCanonicalFrame> {
    if !context.is_valid()
        || world.width() == 0
        || world.height() == 0
        || reachable.len() != world.cell_count()
    {
        return None;
    }

    let origin_index = cell_index(origin, world).ok()?;
    if !reachable[origin_index] {
        return None;
    }

    let mut component = vec![false; world.cell_count()];
    let mut pending = vec![origin];
    component[origin_index] = true;
    let (mut min_x, mut min_y) = world.coordinates(origin)?;
    let (mut max_x, mut max_y) = (min_x, min_y);

    while let Some(cell) = pending.pop() {
        let (x, y) = world.coordinates(cell)?;
        min_x = min_x.min(x);
        min_y = min_y.min(y);
        max_x = max_x.max(x);
        max_y = max_y.max(y);
        for neighbour in world.neighbours4(cell).into_iter().flatten() {
            let index = cell_index(neighbour, world).ok()?;
            if reachable[index] && !component[index] {
                component[index] = true;
                pending.push(neighbour);
            }
        }
    }

    let width = max_x.checked_sub(min_x)?.checked_add(1)?;
    let height = max_y.checked_sub(min_y)?.checked_add(1)?;
    let mut best_signature: Option<Vec<u16>> = None;
    let mut reflections = Vec::new();
    for reflection in GRID_REFLECTIONS {
        let signature = reflected_local_context_signature(
            world,
            region,
            context,
            &component,
            min_x,
            min_y,
            width,
            height,
            reflection,
        )?;
        match best_signature.as_ref() {
            None => {
                best_signature = Some(signature);
                reflections.push(reflection);
            }
            Some(best) => match signature.cmp(best) {
                Ordering::Less => {
                    best_signature = Some(signature);
                    reflections.clear();
                    reflections.push(reflection);
                }
                Ordering::Equal => reflections.push(reflection),
                Ordering::Greater => {}
            },
        }
    }

    (!reflections.is_empty()).then_some(TemporaryDestinationCanonicalFrame {
        min_x,
        min_y,
        width,
        height,
        reflections,
    })
}

pub(crate) fn destination_coupling_classes(
    world: &World,
    frame: &TemporaryDestinationCanonicalFrame,
    origin: CellId,
    candidates: &[TemporaryTravelDestinationCandidate],
) -> Option<Vec<u32>> {
    let origin_local = local_coordinates(world, frame, origin)?;
    let mut keyed = Vec::with_capacity(candidates.len());
    for (index, candidate) in candidates.iter().enumerate() {
        let destination_local = local_coordinates(world, frame, candidate.destination)?;
        let mut best_pair: Option<(u32, u32, u32, u32, u32)> = None;
        for &reflection in &frame.reflections {
            let mapped_origin = reflected_coordinates(
                reflection,
                origin_local.0,
                origin_local.1,
                frame.width,
                frame.height,
            );
            let mapped_destination = reflected_coordinates(
                reflection,
                destination_local.0,
                destination_local.1,
                frame.width,
                frame.height,
            );
            let key = (
                mapped_origin.0,
                mapped_origin.1,
                mapped_destination.0,
                mapped_destination.1,
                candidate.route_distance_edges,
            );
            best_pair = Some(best_pair.map_or(key, |prior| prior.min(key)));
        }
        keyed.push((best_pair?, index));
    }
    keyed.sort_unstable_by_key(|(key, index)| (*key, *index));

    let mut classes = vec![0_u32; candidates.len()];
    let mut previous = None;
    let mut class = 0_u32;
    for (key, index) in keyed {
        if previous.is_some_and(|prior| prior != key) {
            class = class.checked_add(1)?;
        }
        classes[index] = class;
        previous = Some(key);
    }
    Some(classes)
}

#[allow(clippy::too_many_arguments)]
fn reflected_local_context_signature(
    world: &World,
    region: &FocalRegion,
    context: TemporaryDestinationCouplingContext,
    component: &[bool],
    min_x: u32,
    min_y: u32,
    width: u32,
    height: u32,
    reflection: GridReflection,
) -> Option<Vec<u16>> {
    let fields_per_slot = 3
        + if context.include_water_access { 1 } else { 0 }
        + if context.include_base_productivity { 1 } else { 0 };
    let slot_count = usize::try_from(width.checked_mul(height)?).ok()?;
    let capacity = slot_count.checked_mul(fields_per_slot)?;
    let mut signature = Vec::with_capacity(capacity);

    for y in 0..height {
        for x in 0..width {
            let (source_x, source_y) = reflected_coordinates(reflection, x, y, width, height);
            let source = world.cell_id(min_x.checked_add(source_x)?, min_y.checked_add(source_y)?)?;
            let source_index = cell_index(source, world).ok()?;
            if !component.get(source_index).copied().unwrap_or(false) {
                signature.push(0);
                signature.push(0);
                signature.push(0);
                if context.include_water_access {
                    signature.push(0);
                }
                if context.include_base_productivity {
                    signature.push(0);
                }
                continue;
            }

            let cell = world.cell(source)?;
            signature.push(1);
            signature.push(if region.contains(source) { 1 } else { 0 });
            signature.push(cell.movement_cost);
            if context.include_water_access {
                signature.push(cell.water_access);
            }
            if context.include_base_productivity {
                signature.push(cell.base_productivity);
            }
        }
    }
    Some(signature)
}

fn local_coordinates(
    world: &World,
    frame: &TemporaryDestinationCanonicalFrame,
    cell: CellId,
) -> Option<(u32, u32)> {
    let (x, y) = world.coordinates(cell)?;
    let local_x = x.checked_sub(frame.min_x)?;
    let local_y = y.checked_sub(frame.min_y)?;
    (local_x < frame.width && local_y < frame.height).then_some((local_x, local_y))
}

fn reflected_coordinates(
    reflection: GridReflection,
    x: u32,
    y: u32,
    width: u32,
    height: u32,
) -> (u32, u32) {
    match reflection {
        GridReflection::Identity => (x, y),
        GridReflection::Horizontal => (width - 1 - x, y),
        GridReflection::Vertical => (x, height - 1 - y),
        GridReflection::Both => (width - 1 - x, height - 1 - y),
    }
}

'''
          text = text[:start] + replacement + text[end:]

          old = '''        let canonical_frame =
            destination_canonical_frame(world, region, destination_coupling_context)
                .ok_or(TemporaryTravelModelError::InvalidDestinationCouplingContext)?;

        let labels = minimum_cost_labels(self, region, world)?;
'''
          new = '''        let labels = minimum_cost_labels(self, region, world)?;
        let reachable = labels.iter().map(Option::is_some).collect::<Vec<_>>();
'''
          assert old in text
          text = text.replace(old, new, 1)

          old = '''            let classes =
                destination_coupling_classes(world, &canonical_frame, origin, &candidates).ok_or(
                    TemporaryTravelModelError::DestinationCouplingClassificationFailed { origin },
                )?;
'''
          new = '''            let canonical_frame = destination_canonical_frame(
                world,
                region,
                destination_coupling_context,
                &reachable,
                origin,
            )
            .ok_or(TemporaryTravelModelError::DestinationCouplingClassificationFailed { origin })?;
            let classes = destination_coupling_classes(world, &canonical_frame, origin, &candidates)
                .ok_or(TemporaryTravelModelError::DestinationCouplingClassificationFailed {
                    origin,
                })?;
'''
          assert old in text
          text = text.replace(old, new, 1)
          travel.write_text(text)

          mobility = Path('crates/anthrosim-core/src/temporary_mobility.rs')
          text = mobility.read_text()
          text = text.replace(
              'm9/equal-cost-destination-local-household-spatial-equivalence-v4',
              'm9/equal-cost-destination-local-household-spatial-equivalence-v5',
              1,
          )
          text = text.replace('pub const CURRENT_SCHEMA_VERSION: u32 = 5;', 'pub const CURRENT_SCHEMA_VERSION: u32 = 6;', 1)
          text = text.replace(
              'The class is derived from an exact canonical reflection frame. Canonical CellId is\n    /// used only to serialize members inside one class, where they are scientifically',
              'The class is derived from an exact reflection-canonical frame over the origin\'s reachable\n    /// traversable component. Canonical CellId is used only to serialize members inside one class, where they are scientifically',
              1,
          )

          old = '''                let frame = destination_canonical_frame(world, region, context)
                    .ok_or(TemporaryMobilityProgramError::InvalidDestinationCouplingContext)?;
                if costs.len() != world.cell_count() {
'''
          new = '''                if costs.len() != world.cell_count() {
'''
          assert old in text
          text = text.replace(old, new, 1)

          old = '''                Some((model, costs, candidates, frame))
'''
          new = '''                let reachable = costs.iter().map(Option::is_some).collect::<Vec<_>>();
                Some((model, costs, candidates, context, reachable))
'''
          assert old in text
          text = text.replace(old, new, 1)

          old = '''            if let Some((model, costs, candidate_rows, frame)) = m9_4.as_ref() {
'''
          new = '''            if let Some((model, costs, candidate_rows, context, reachable)) = m9_4.as_ref() {
'''
          assert old in text
          text = text.replace(old, new, 1)

          old = '''                        let expected_classes =
                            destination_coupling_classes(world, frame, origin, candidates).ok_or(
                                TemporaryMobilityProgramError::InvalidDestinationCouplingClasses {
                                    origin,
                                },
                            )?;
'''
          new = '''                        let frame = destination_canonical_frame(
                            world,
                            region,
                            *context,
                            reachable,
                            origin,
                        )
                        .ok_or(TemporaryMobilityProgramError::InvalidDestinationCouplingClasses {
                            origin,
                        })?;
                        let expected_classes = destination_coupling_classes(
                            world,
                            &frame,
                            origin,
                            candidates,
                        )
                        .ok_or(TemporaryMobilityProgramError::InvalidDestinationCouplingClasses {
                            origin,
                        })?;
'''
          assert old in text
          text = text.replace(old, new, 1)
          mobility.write_text(text)

          provenance = Path('crates/anthrosim-core/src/provenance.rs')
          text = provenance.read_text()
          marker = 'pub const MODEL_SEMANTICS_ID: &str = "anthrosim-model-semantics-v37";'
          assert marker in text
          note = '''///\n/// v38 localizes M9 equal-cost destination spatial coupling to the origin's reachable traversable\n/// component and normalizes that component to local coordinates before reflection canonicalization.\n/// Causally isolated unreachable/impassable world padding therefore cannot relabel an unchanged\n/// local tie, while reachable route-changing extent remains scientifically causal. The persisted M9\n/// travel-table coupling classes and tie-policy identity advance accordingly. A v37 checkpoint must\n/// not resume under v38 while silently changing future tied M9 destinations.\n'''
          text = text.replace(marker, note + 'pub const MODEL_SEMANTICS_ID: &str = "anthrosim-model-semantics-v38";', 1)
          provenance.write_text(text)
          PY

          cargo fmt --all -- --check || cargo fmt --all
          git diff --check
          git status --short
      - name: Commit repair and remove staging workflow
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm .github/workflows/apply-av6-006-local-tie-repair.yml
          git add crates/anthrosim-core/src/temporary_travel.rs crates/anthrosim-core/src/temporary_mobility.rs crates/anthrosim-core/src/provenance.rs .github/workflows/apply-av6-006-local-tie-repair.yml
          git commit -m "fix(audit-v6): localize M9 destination tie frame"
          git push origin HEAD:audit/repair-711-av6-006
