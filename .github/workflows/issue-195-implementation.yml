name: issue-195-implementation
on:
  push:
    branches:
      - fix/195-m4-proportional-weights
    paths:
      - .github/workflows/issue-195-implementation.yml
      - .github/issue-195-trace-apply.py
permissions:
  contents: write
jobs:
  implement-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97.1
          components: rustfmt, clippy
      - name: Apply complete trace repair
        run: python .github/issue-195-trace-apply.py
      - name: Format
        run: cargo fmt --all
      - name: Core migration tests
        run: cargo test --locked -p anthrosim-core migration::tests -- --nocapture
      - name: Migration integration tests
        run: cargo test --locked -p anthrosim-core --test migration_behavior -- --nocapture
      - name: Core Clippy
        run: cargo clippy --locked -p anthrosim-core --all-targets --all-features -- -D warnings
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/anthrosim-core/src/migration.rs crates/anthrosim-core/src/lib.rs crates/anthrosim-core/src/checkpoint.rs crates/anthrosim-core/src/manifest.rs crates/anthrosim-core/tests/migration_behavior.rs docs/research/migration-v0.1.md docs/scientific-model.md docs/research/trace-m4-proportional-choice-repair-2026-08-27.md
          git commit -m "test: preserve complete M4 choice weights for issue #195"
          git push
