name: Temporary #211 boundary validation

on:
  push:
    branches: [feat/211-spatial-boundary-contract]

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout branch
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0
        with:
          ref: feat/211-spatial-boundary-contract
          fetch-depth: 0

      - name: Install pinned Rust toolchain
        run: |
          rustup set profile minimal
          rustup toolchain install 1.97.1 --component clippy --component rustfmt
          rustup default 1.97.1

      - name: Add convergence evaluator and repair physical-route comparison
        run: |
          python3 - <<'PY'
          from pathlib import Path

          module = Path('crates/anthrosim-core/src/spatial_boundary.rs')
          text = module.read_text()
          marker = '\nfn simulation_extent(\n'
          if 'pub struct SpatialExtentConvergenceAssessment' not in text:
              insertion = r'''

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SpatialExtentMetricObservation {
    pub metric_id: String,
    pub value: u64,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SpatialExtentObservation {
    pub buffer_cells: u32,
    pub metrics: Vec<SpatialExtentMetricObservation>,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SpatialExtentMetricComparison {
    pub metric_id: String,
    pub previous_value: u64,
    pub current_value: u64,
    pub absolute_difference: u64,
    pub relative_difference_permille: u16,
    pub absolute_within_tolerance: Option<bool>,
    pub relative_within_tolerance: Option<bool>,
    pub within_tolerance: bool,
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SpatialExtentExtensionComparison {
    pub previous_buffer_cells: u32,
    pub current_buffer_cells: u32,
    pub eligible_for_stability_sequence: bool,
    pub all_metrics_within_tolerance: bool,
    pub metrics: Vec<SpatialExtentMetricComparison>,
}

/// Machine-readable evaluation of a predeclared extent-adequacy criterion.
///
/// This compares adjacent enlargements of one fixed physical analysis domain. `adequate` is true
/// only when the required number of *trailing* eligible enlargements all satisfy every declared
/// metric tolerance. It does not imply that unmeasured outputs are boundary-insensitive.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SpatialExtentConvergenceAssessment {
    pub schema_version: u32,
    pub criterion: SpatialExtentAdequacyCriterion,
    pub observations: Vec<SpatialExtentObservation>,
    pub comparisons: Vec<SpatialExtentExtensionComparison>,
    pub trailing_stable_extensions: u16,
    pub latest_extension_within_tolerance: bool,
    pub latest_extension_eligible_for_stability_sequence: bool,
    pub material_boundary_dependence_at_latest_extension: bool,
    pub adequate: bool,
}

impl SpatialExtentConvergenceAssessment {
    pub const CURRENT_SCHEMA_VERSION: u32 = 1;
}

/// Evaluate observed inner-domain metrics from progressively enlarged simulation domains.
///
/// Buffers must be strictly increasing and every observation must contain exactly the metrics
/// named by the criterion. Relative differences use `max(previous, current)` as a symmetric
/// denominator and round upward to whole permille so a small non-zero change cannot disappear by
/// integer truncation. If both absolute and relative tolerances are declared, both must pass.
pub fn assess_spatial_extent_convergence(
    criterion: SpatialExtentAdequacyCriterion,
    observations: Vec<SpatialExtentObservation>,
) -> Result<SpatialExtentConvergenceAssessment, SpatialBoundaryError> {
    criterion.validate()?;
    if observations.len() < 2 {
        return Err(SpatialBoundaryError::TooFewExtentObservations);
    }

    let expected_metric_ids = criterion
        .metric_tolerances
        .iter()
        .map(|tolerance| tolerance.metric_id.as_str())
        .collect::<BTreeSet<_>>();
    for (index, observation) in observations.iter().enumerate() {
        if index > 0 && observation.buffer_cells <= observations[index - 1].buffer_cells {
            return Err(SpatialBoundaryError::NonIncreasingExtentBuffers {
                previous: observations[index - 1].buffer_cells,
                current: observation.buffer_cells,
            });
        }
        let mut observed_ids = BTreeSet::new();
        for metric in &observation.metrics {
            if metric.metric_id.trim().is_empty() {
                return Err(SpatialBoundaryError::EmptyExtentObservationMetricId);
            }
            if !observed_ids.insert(metric.metric_id.as_str()) {
                return Err(SpatialBoundaryError::DuplicateExtentObservationMetricId(
                    metric.metric_id.clone(),
                ));
            }
            if !expected_metric_ids.contains(metric.metric_id.as_str()) {
                return Err(SpatialBoundaryError::UnexpectedExtentObservationMetric(
                    metric.metric_id.clone(),
                ));
            }
        }
        for expected in &expected_metric_ids {
            if !observed_ids.contains(expected) {
                return Err(SpatialBoundaryError::MissingExtentObservationMetric(
                    (*expected).to_owned(),
                ));
            }
        }
    }

    let mut comparisons = Vec::with_capacity(observations.len() - 1);
    for pair in observations.windows(2) {
        let previous = &pair[0];
        let current = &pair[1];
        let mut metric_comparisons = Vec::with_capacity(criterion.metric_tolerances.len());
        let mut all_metrics_within_tolerance = true;
        for tolerance in &criterion.metric_tolerances {
            let previous_value = previous
                .metrics
                .iter()
                .find(|metric| metric.metric_id == tolerance.metric_id)
                .expect("validated extent observation metric must exist")
                .value;
            let current_value = current
                .metrics
                .iter()
                .find(|metric| metric.metric_id == tolerance.metric_id)
                .expect("validated extent observation metric must exist")
                .value;
            let absolute_difference = previous_value.abs_diff(current_value);
            let denominator = previous_value.max(current_value);
            let relative_difference_permille = if denominator == 0 {
                0
            } else {
                let numerator = u128::from(absolute_difference) * 1_000;
                let denominator = u128::from(denominator);
                let rounded_up = numerator.div_ceil(denominator).min(1_000);
                u16::try_from(rounded_up).map_err(|_| SpatialBoundaryError::AccountingOverflow)?
            };
            let absolute_within_tolerance = tolerance
                .max_absolute_difference
                .map(|limit| absolute_difference <= limit);
            let relative_within_tolerance = tolerance
                .max_relative_difference_permille
                .map(|limit| relative_difference_permille <= limit);
            let within_tolerance = absolute_within_tolerance.unwrap_or(true)
                && relative_within_tolerance.unwrap_or(true);
            all_metrics_within_tolerance &= within_tolerance;
            metric_comparisons.push(SpatialExtentMetricComparison {
                metric_id: tolerance.metric_id.clone(),
                previous_value,
                current_value,
                absolute_difference,
                relative_difference_permille,
                absolute_within_tolerance,
                relative_within_tolerance,
                within_tolerance,
            });
        }
        let eligible_for_stability_sequence = criterion
            .minimum_buffer_cells
            .is_none_or(|minimum| previous.buffer_cells >= minimum);
        comparisons.push(SpatialExtentExtensionComparison {
            previous_buffer_cells: previous.buffer_cells,
            current_buffer_cells: current.buffer_cells,
            eligible_for_stability_sequence,
            all_metrics_within_tolerance,
            metrics: metric_comparisons,
        });
    }

    let trailing_stable_extensions_usize = comparisons
        .iter()
        .rev()
        .take_while(|comparison| {
            comparison.eligible_for_stability_sequence && comparison.all_metrics_within_tolerance
        })
        .count();
    let trailing_stable_extensions = u16::try_from(trailing_stable_extensions_usize)
        .unwrap_or(u16::MAX);
    let latest = comparisons
        .last()
        .expect("two observations must produce one comparison");
    let adequate = trailing_stable_extensions >= criterion.required_consecutive_stable_extensions;

    Ok(SpatialExtentConvergenceAssessment {
        schema_version: SpatialExtentConvergenceAssessment::CURRENT_SCHEMA_VERSION,
        criterion,
        observations,
        comparisons,
        trailing_stable_extensions,
        latest_extension_within_tolerance: latest.all_metrics_within_tolerance,
        latest_extension_eligible_for_stability_sequence: latest.eligible_for_stability_sequence,
        material_boundary_dependence_at_latest_extension: !latest.all_metrics_within_tolerance,
        adequate,
    })
}
'''
              if marker not in text:
                  raise SystemExit('spatial_boundary insertion marker not found')
              text = text.replace(marker, insertion + marker, 1)
              error_marker = '    #[error("spatial coordinate extent overflowed")]\n    CoordinateExtentOverflow,'
              error_insertion = r'''    #[error("spatial extent convergence requires at least two observations")]
    TooFewExtentObservations,
    #[error("spatial extent buffers must increase strictly; previous {previous}, current {current}")]
    NonIncreasingExtentBuffers { previous: u32, current: u32 },
    #[error("spatial extent observation metric identifier is empty")]
    EmptyExtentObservationMetricId,
    #[error("duplicate spatial extent observation metric identifier {0}")]
    DuplicateExtentObservationMetricId(String),
    #[error("unexpected spatial extent observation metric {0}")]
    UnexpectedExtentObservationMetric(String),
    #[error("missing spatial extent observation metric {0}")]
    MissingExtentObservationMetric(String),
    #[error("spatial coordinate extent overflowed")]
    CoordinateExtentOverflow,'''
              if error_marker not in text:
                  raise SystemExit('spatial_boundary error marker not found')
              text = text.replace(error_marker, error_insertion, 1)
              module.write_text(text)

          lib = Path('crates/anthrosim-core/src/lib.rs')
          text = lib.read_text()
          old = '''    SpatialBoundaryInterpretation, SpatialBoundarySemantics, SpatialExtentAdequacyCriterion,
    SpatialExtentMetricTolerance, assess_spatial_boundary,
'''
          new = '''    SpatialBoundaryInterpretation, SpatialBoundarySemantics, SpatialExtentAdequacyCriterion,
    SpatialExtentConvergenceAssessment, SpatialExtentExtensionComparison,
    SpatialExtentMetricComparison, SpatialExtentMetricObservation, SpatialExtentMetricTolerance,
    SpatialExtentObservation, assess_spatial_boundary, assess_spatial_extent_convergence,
'''
          if old not in text:
              raise SystemExit('lib export marker not found')
          lib.write_text(text.replace(old, new, 1))

          tests = Path('crates/anthrosim-core/tests/spatial_boundary_dependence.rs')
          text = tests.read_text()
          old_import = '''    SpatialBoundaryDeclaration, SpatialBoundaryError, SpatialBoundaryInterpretation,
    SpatialExtentAdequacyCriterion, SpatialExtentMetricTolerance, SpatialFieldTransform,
'''
          new_import = '''    SpatialBoundaryDeclaration, SpatialBoundaryError, SpatialBoundaryInterpretation,
    SpatialExtentAdequacyCriterion, SpatialExtentMetricObservation, SpatialExtentMetricTolerance,
    SpatialExtentObservation, SpatialFieldTransform,
'''
          if old_import not in text:
              raise SystemExit('test import marker not found')
          text = text.replace(old_import, new_import, 1)
          text = text.replace(
              '    assess_spatial_boundary, bounded_candidate_cells, derive_temporary_mobility_observability,\n',
              '    assess_spatial_boundary, assess_spatial_extent_convergence, bounded_candidate_cells,\n    derive_temporary_mobility_observability,\n',
              1,
          )
          old_route = '''        let table = model.derive_table(&region, &world).unwrap();
        (
            table.resolution(origin).unwrap(),
            table.accumulated_cost_units(origin),
        )
    };

    let tight_resolution = resolution(&tight);
    assert_eq!(tight_resolution.0, TemporaryTravelResolution::Unreachable);
    assert_eq!(tight_resolution.1, None);
    let buffered_resolution = resolution(&buffered);
    let larger_resolution = resolution(&larger);
    assert_eq!(buffered_resolution, larger_resolution);
    assert!(matches!(
        buffered_resolution.0,
        TemporaryTravelResolution::Reachable {
            outbound_travel_days: 3,
            return_travel_days: 3,
            ..
        }
    ));
    assert_eq!(buffered_resolution.1, Some(8_000));
'''
          new_route = '''        let table = model.derive_table(&region, &world).unwrap();
        let resolution = table.resolution(origin).unwrap();
        let destination_extent = match resolution {
            TemporaryTravelResolution::Reachable { destination, .. } => {
                Some(landscape.cell_extent(destination).unwrap())
            }
            TemporaryTravelResolution::Unreachable => None,
        };
        (
            resolution,
            table.accumulated_cost_units(origin),
            destination_extent,
        )
    };

    let tight_resolution = resolution(&tight);
    assert_eq!(tight_resolution.0, TemporaryTravelResolution::Unreachable);
    assert_eq!(tight_resolution.1, None);
    assert_eq!(tight_resolution.2, None);
    let buffered_resolution = resolution(&buffered);
    let larger_resolution = resolution(&larger);
    assert_eq!(buffered_resolution.1, larger_resolution.1);
    assert_eq!(buffered_resolution.2, larger_resolution.2);
    for resolution in [buffered_resolution.0, larger_resolution.0] {
        assert!(matches!(
            resolution,
            TemporaryTravelResolution::Reachable {
                outbound_travel_days: 3,
                return_travel_days: 3,
                ..
            }
        ));
    }
    assert_eq!(buffered_resolution.1, Some(8_000));
'''
          if old_route not in text:
              raise SystemExit('physical route block not found')
          text = text.replace(old_route, new_route, 1)
          if 'study_specific_convergence_evaluator_reports_trailing_stability' not in text:
              text += r'''

#[test]
fn study_specific_convergence_evaluator_reports_trailing_stability() {
    let criterion = SpatialExtentAdequacyCriterion {
        schema_version: SpatialExtentAdequacyCriterion::CURRENT_SCHEMA_VERSION,
        criterion_id: "fixed-inner-visitation-v1".to_owned(),
        required_consecutive_stable_extensions: 2,
        minimum_buffer_cells: Some(1),
        metric_tolerances: vec![SpatialExtentMetricTolerance {
            metric_id: "visitor_person_days".to_owned(),
            max_absolute_difference: Some(0),
            max_relative_difference_permille: Some(0),
        }],
    };
    let observation = |buffer_cells, value| SpatialExtentObservation {
        buffer_cells,
        metrics: vec![SpatialExtentMetricObservation {
            metric_id: "visitor_person_days".to_owned(),
            value,
        }],
    };
    let assessment = assess_spatial_extent_convergence(
        criterion,
        vec![
            observation(0, 0),
            observation(1, 5),
            observation(2, 5),
            observation(3, 5),
        ],
    )
    .unwrap();

    assert!(!assessment.comparisons[0].all_metrics_within_tolerance);
    assert!(!assessment.comparisons[0].eligible_for_stability_sequence);
    assert_eq!(
        assessment.comparisons[0].metrics[0].relative_difference_permille,
        1_000
    );
    assert!(assessment.comparisons[1].all_metrics_within_tolerance);
    assert!(assessment.comparisons[1].eligible_for_stability_sequence);
    assert!(assessment.comparisons[2].all_metrics_within_tolerance);
    assert_eq!(assessment.trailing_stable_extensions, 2);
    assert!(assessment.latest_extension_within_tolerance);
    assert!(!assessment.material_boundary_dependence_at_latest_extension);
    assert!(assessment.adequate);
}

#[test]
fn convergence_relative_difference_rounds_up_and_all_declared_tolerances_must_pass() {
    let criterion = SpatialExtentAdequacyCriterion {
        schema_version: SpatialExtentAdequacyCriterion::CURRENT_SCHEMA_VERSION,
        criterion_id: "strict-dual-tolerance-v1".to_owned(),
        required_consecutive_stable_extensions: 1,
        minimum_buffer_cells: None,
        metric_tolerances: vec![SpatialExtentMetricTolerance {
            metric_id: "migration_count".to_owned(),
            max_absolute_difference: Some(1),
            max_relative_difference_permille: Some(0),
        }],
    };
    let assessment = assess_spatial_extent_convergence(
        criterion,
        vec![
            SpatialExtentObservation {
                buffer_cells: 1,
                metrics: vec![SpatialExtentMetricObservation {
                    metric_id: "migration_count".to_owned(),
                    value: 1_000,
                }],
            },
            SpatialExtentObservation {
                buffer_cells: 2,
                metrics: vec![SpatialExtentMetricObservation {
                    metric_id: "migration_count".to_owned(),
                    value: 1_001,
                }],
            },
        ],
    )
    .unwrap();

    let metric = &assessment.comparisons[0].metrics[0];
    assert_eq!(metric.absolute_difference, 1);
    assert_eq!(metric.relative_difference_permille, 1);
    assert_eq!(metric.absolute_within_tolerance, Some(true));
    assert_eq!(metric.relative_within_tolerance, Some(false));
    assert!(!metric.within_tolerance);
    assert!(assessment.material_boundary_dependence_at_latest_extension);
    assert!(!assessment.adequate);
}
'''
          tests.write_text(text)
          PY
          cargo fmt --all
          cargo fmt --all -- --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/anthrosim-core/src/spatial_boundary.rs crates/anthrosim-core/src/lib.rs crates/anthrosim-core/tests/spatial_boundary_dependence.rs
          git commit -m "feat: evaluate spatial extent convergence" || true
          git push origin HEAD:feat/211-spatial-boundary-contract

      - name: Validate boundary contract
        run: |
          cargo clippy --locked --workspace --all-targets --all-features -- -D warnings
          cargo test --locked -p anthrosim-core --test spatial_boundary_dependence
          cargo build --locked --workspace --release

      - name: Remove temporary workflow
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/tmp-211-format-validation.yml
          git commit -m "chore: remove temporary #211 validation workflow"
          git push origin HEAD:feat/211-spatial-boundary-contract
