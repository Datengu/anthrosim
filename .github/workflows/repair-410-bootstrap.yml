name: Bootstrap repair 410 independent-arm design
on:
  push:
    branches:
      - fix/410-independent-arm-seeds
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/410-independent-arm-seeds
      - run: python3 scripts/apply-repair-410-bootstrap.py
      - run: |
          rm scripts/apply-repair-410-bootstrap.py
          rm .github/workflows/repair-410-bootstrap.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'fix: make independent Monte Carlo arms seed-disjoint'
          git push origin HEAD:fix/410-independent-arm-seeds
