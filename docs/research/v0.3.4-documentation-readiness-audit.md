# AnthroSim v0.3.4 documentation-readiness audit

Date: 2026-09-02  
Privacy-sanitized entry protected-main identity: `fa85fbf5003ac4e8cd29fda8c1a3c6b9a02c1f25` (rewritten equivalent of the pre-sanitisation entry state)  
Previous release: `v0.3.3` -> rewritten live tag `d3b6fc0b0750933b57252c9087513d156d88f218`  
Entry/current executable model semantics: `anthrosim-model-semantics-v25`

## Purpose

Perform the pre-release documentation/version-identity convergence pass for `v0.3.4` after Scientific Audit v3 remediation. This is **not** Audit v4 and does not rerun Audit v3 discovery. Its purpose is to ensure that living/release-facing documentation describes the state that will actually be frozen while preserving the scientific meaning of historical audit evidence after the explicitly authorized 2026-09-02 privacy history rewrite.

## Repository state at entry

- Scientific Audit v3 discovery is complete against the v0.3.3/v21 release baseline;
- Audit v3 demonstrated 17 findings: 1 P0, 6 P1 and 10 P2;
- all 17 original findings have dedicated production repairs and independent post-merge adversarial reverification;
- all 17 original Audit-v3 issues are closed;
- there were zero open issues carrying `audit: v3` and zero open pull requests at the initial release-preparation entry point;
- the privacy-sanitized rewritten equivalent of that entry protected-main state is `fa85fbf5003ac4e8cd29fda8c1a3c6b9a02c1f25`;
- current model semantics are v25;
- v0.3.3 retains its release/version identity and v21 model semantics; its live rewritten tag now resolves to `d3b6fc0b0750933b57252c9087513d156d88f218`.

## Comprehensive documentation classification

### Living/release-facing surfaces updated for the freeze

- `Cargo.toml` and workspace entries in `Cargo.lock`: 0.3.3 → 0.3.4;
- `CITATION.cff`: v0.3.4, 2026-09-02, Audit-v3/v25 abstract;
- `README.md`: current release baseline, capabilities/milestone status and next-phase wording;
- `docs/roadmap.md`: post-M9 direction now records Audit-v3 completion and the v0.3.4/v25 freeze;
- `docs/research/README.md`: research-document index now describes Audit v3 as completed, distinguishes the historical discovery ledger from current remediation status, and links this v0.3.4 readiness record;
- `docs/research/trace.md`: framework baseline and TRACE 2/4/5 convergence claims now reflect Audit v3 rather than treating it as future work;
- `docs/research/odd.md`, `docs/research/odd-d.md`, `docs/scientific-model.md`: living baseline headers identify v0.3.4/v25 while preserving v0.3.3/v21 release semantics;
- `docs/release-versioning.md`: adds v0.3.4, records that the release bump itself does not change v25 semantics, and records the exceptional privacy-driven rewrite policy;
- `docs/releases/v0.3.4.md`: new release note and pre-tag gate.

### Checked and intentionally unchanged in scientific meaning

- `SECURITY.md`: support policy is main-based rather than hard-coded to an old release, and the RustSec release requirement remains appropriate;
- generic provenance, experiment, benchmark and mechanism contracts whose version identifiers are schema/model-contract identities rather than software-release numbers;
- the generic `release-tag.yml` policy, which validates package/citation/release-note identity plus protected and release-specific exact-SHA gates.

### Historical records preserved through the privacy rewrite

Historical audit and release records retain their scientific conclusions, release/version identities and model-semantics identities. On 2026-09-02, an explicitly authorized repository-wide privacy sanitisation rewrote Git object identities and sanitized private textual references; living provenance was subsequently reconciled to reachable rewritten identities. This was provenance/privacy maintenance, not a new scientific validation pass.

This applies to:

- `docs/releases/v0.3.3.md` and earlier release records;
- `docs/research/v0.3.3-documentation-readiness-audit.md`;
- `docs/research/audit-v3/README.md`, `STATUS.md` and dated Area A–N/discovery evidence;
- historical Audit-v2 records and older TRACE repair records;
- the v0.3.3 release identity and its model-semantics-v21 baseline.

Historical v0.3.3/v21 text inside those records is provenance, not current-documentation drift. Where exact Git identities are needed, the rewritten reachable identities are authoritative for the sanitized repository.

## Release identity decision

`v0.3.4` is the correct next software version because the latest existing SemVer tag is `v0.3.3`. The release is a patch, not a new minor milestone: it freezes compatible scientific-hardening/remediation work completed after v0.3.3. The release-preparation change itself does not alter causal simulator behaviour, so `MODEL_SEMANTICS_ID` remains `anthrosim-model-semantics-v25`.

## Pre-tag checklist

After this release-preparation PR is merged, the exact resulting protected-main SHA must still satisfy the repository's release policy:

- all protected required contexts green on the exact candidate SHA;
- exact-SHA M8.6 benchmark disposition green;
- exact-SHA M9.7 benchmark disposition green;
- exact-SHA RustSec dependency audit green;
- root package version = 0.3.4;
- `CITATION.cff` version = 0.3.4;
- `docs/releases/v0.3.4.md` present;
- current `main` HEAD equals the candidate SHA;
- no unresolved release-blocking issue or PR.

Only then should `refs/tags/v0.3.4` be created through the repository's immutable-intent release-tag workflow.

## Conclusion

The documentation/versioning changes in this freeze are maintenance/provenance changes over the already-reviewed v25 scientific state. They do not upgrade AnthroSim to empirical archaeological validity. Subject to exact-head protected and release-specific gates, this repository state is suitable to freeze as the named v0.3.4 post-Audit-v3 convergence baseline.
