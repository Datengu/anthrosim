# Scientific audit v2 — Area M: documentation, TRACE/ODD/ODD+D, claim consistency

Date: 2026-08-29

## Baseline and overlap

- Executable protected-main baseline: `e528fb990acccd1b1a492c67216b6fa7cc64d67b`.
- Model semantics: `anthrosim-model-semantics-v19`.
- Area K/L recording work is documentation-only and does not alter executable semantics.
- No Area M audit branch existed at entry.

## Known documentation dependencies rechecked rather than duplicated

Earlier audit-v2 areas already established several current-facing documentation/result inconsistencies:

- #314: current-facing model-semantics identity drift relative to executable v19;
- #315: the M2 demographic-time contract retains superseded mortality execution language;
- #327: active M9 travel documentation still describes lower-CellId equal-cost selection although executable behavior is keyed/order-fair;
- #329: current M9.7 narrative provenance/statistics are stale relative to the checked-in reference result.

These remain Area M findings and were not reopened as duplicate tickets.

The broader research documentation was also read against the current audit evidence with the anti-bias rule that software-method quality must not be upgraded into empirical archaeological validation. No new claim was found that justifies treating AnthroSim as empirically validated for site or another case merely because synthetic/reproducibility gates pass.

## New current-contract drift

`crates/anthrosim-cli/src/sweep.rs` currently defines:

```rust
DERIVED_ANALYSIS_SCHEMA_VERSION = 5
DERIVED_POINT_ANALYSIS_SCHEMA_VERSION = 6
```

Current run rows / summary therefore identify as schema v5 and point rows as schema v6.

However the current-facing `docs/experiments-v0.1.md` says:

> The run-row and top-level analysis-summary wire contracts remain derived-analysis schema v4. The point-table contract is schema v5 ...

That normative text is one generation behind the executable wire contract: documented **v4/v5** versus emitted **v5/v6**.

## Finding

**AV2-014 — P3 — documented sweep derived-analysis schema versions lag current executable wire contracts.**

Issue: #344.

Consequence is reproducibility/analysis-contract confusion rather than a changed scientific trajectory: a downstream consumer built from the current documentation can reject valid artifacts, select a stale schema parser or report the wrong historical schema identity. Historical artifacts should retain their true version; only current-facing contract text should be synchronized.

## Documentation anti-bias observations

- Synthetic validation, software verification and research-method gates remain conceptually distinct from empirical archaeological corroboration in the inspected current framework.
- Existing Area H/J defects (#334/#338) mean current documentation must not imply that a reproducible/identified stochastic result is automatically statistically precise.
- Existing Area K #340 means exact downstream replay currently proves the recorded command/output lineage but not that the separately recorded machine-readable `arguments` object was the configuration actually executed.
- Existing Area L #342 means migration-quality point summaries require explicit weighting semantics before narrative comparison.

These are dependencies on claim wording, not separate Area M issues.

## Repeat conditions

After #344 repair, compare all current-facing sweep-analysis schema-version statements against representative current serialized artifacts or source constants. Do not rewrite older result/provenance files to newer identities.

## Handoff

Area M is complete with #344 plus the previously known #314/#315/#327/#329 documentation findings. Next is **Area N — cross-system integration**, which must explicitly revisit the mandatory interaction matrix and determine which conclusions are blocked pending open P1 repairs.