# AnthroSim v0.3.3 documentation-readiness audit

Date: 2026-09-01  
Audited protected-main SHA: `b576aa999bd9a3baa67fa2f7f20370402ec07bd7`  
Current released baseline: `v0.3.2` -> `eb240ab482d9683b64081d3d1ea8e151592927ee`  
Current executable model semantics: `anthrosim-model-semantics-v21`

## Purpose

Perform a narrow documentation-readiness pass before preparing the named `v0.3.3` release. This is not a new scientific audit and does not reinterpret historical audit records. The goal is to ensure the living project/research documentation is internally consistent with the completed scientific audit v2 and that the release-preparation boundary is explicit before version metadata is changed.

## Repository state at entry

- scientific audit v2 is closed with A–N complete;
- all 15 AV2 findings are repaired;
- no P0/P1/P2/P3 scientific finding remains open;
- current protected main is model semantics v21;
- immutable `v0.3.2` remains model semantics v19;
- issue #317 is closed and path-aware documentation/audit CI is accepted;
- there were zero open issues and zero open pull requests at the start of this pass.

## Correct and release-ready documentation

The following living scientific documents already correctly distinguish current v21 semantics from the immutable v0.3.2/v19 release baseline and do not require semantic correction in this pass:

- `docs/scientific-model.md`;
- `docs/research/odd.md`;
- `docs/research/odd-d.md`;
- `docs/research/audit-v2/STATUS.md`.

The release/versioning policy is also structurally suitable for a new patch release: a named release must be prepared explicitly, package/citation/release-note identities must agree, the candidate must be current protected `main`, all required protected contexts must succeed, and the release-specific M8.6, M9.7 and RustSec dispositions must succeed on the exact release SHA before the immutable-intent tag is created.

## Current-facing drift found

Three living/current-facing surfaces still describe the project primarily in terms of the earlier post-M9 audit backlog rather than the now-completed second independent scientific audit:

1. `README.md`
   - correctly says `v0.3.2` is the latest released baseline;
   - however, its current-development/milestone narrative should explicitly record that scientific audit v2 is complete, all AV2 findings are repaired, protected main is the post-v0.3.2 v21 line, and the next sequence is documentation convergence -> `v0.3.3` release -> fresh independent audit convergence -> question-led model interrogation;
   - wording such as `AnthroSim v0.3.2 includes:` should not imply that post-v0.3.2/v21 repairs belong to the immutable v0.3.2 release.

2. `docs/roadmap.md`
   - correctly rejects a predetermined M10 feature package;
   - its `Direction after M9` section still frames the immediate step as running the next independent audit after the first major post-M9 backlog;
   - it should now record scientific audit v2 as completed and define the immediate sequence as freezing the audited v21 line into `v0.3.3`, then performing another fresh independent/adversarial release-baseline audit before the first empirical case-study inference.

3. `docs/research/trace.md`
   - remains correctly conservative (`NOT YET EMPIRICALLY RESEARCH-READY`);
   - its baseline and TRACE 4/5 summary still refer to the `first major post-M9 scientific-audit backlog` / `one clean backlog`;
   - it should record the completed audit-v2 evidence while retaining the requirement for another independent convergence pass and all study-specific empirical-validation requirements.

## Historical records that must not be rewritten

The following apparently stale statements are intentional historical evidence and should remain as written unless a factual error in the historical record itself is demonstrated:

- dated `docs/research/audit-v2/area-*.md` records describing findings as open at the time they were discovered;
- older TRACE audit/pass records;
- `docs/releases/v0.3.2.md`, which correctly describes the immutable v0.3.2/v19 release state;
- the immutable v0.3.2 tag and its historical model-semantics identity.

Rewriting those files to make historical statements look current would weaken provenance.

## Expected pre-release state, not defects

The following are intentionally still `0.3.2` or absent until the explicit `v0.3.3` release-preparation change and therefore are not documentation defects in this pass:

- root `Cargo.toml` package version remains `0.3.2`;
- `CITATION.cff` remains version `0.3.2` with its v0.3.2 release date/abstract;
- `docs/releases/v0.3.3.md` does not yet exist;
- no `v0.3.3` tag exists.

Those identities should change together in the release-preparation PR rather than opportunistically during this documentation cleanup.

## Required cleanup before v0.3.3 preparation

Before the version-bump/release-candidate PR:

1. reconcile `README.md` to the completed audit-v2/current-v21 development state without relabelling v21 work as part of immutable v0.3.2;
2. reconcile `docs/roadmap.md` so the next sequence is explicit: `v0.3.3` freeze -> fresh independent/adversarial audit -> question-led model interrogation;
3. reconcile the living `docs/research/trace.md` baseline and TRACE 4/5 convergence wording to include audit v2 while remaining empirically conservative;
4. run the scientific-documentation consistency suite and ensure no current-facing model-semantics/version contradiction is introduced.

## v0.3.3 release-preparation checklist after cleanup

The subsequent explicit release-preparation change should, as one coherent release identity update:

- change workspace/package version `0.3.2 -> 0.3.3`;
- update `CITATION.cff` to `0.3.3` and the actual release date/abstract;
- add `docs/releases/v0.3.3.md` describing the post-audit-v2 v21 baseline and interpretation limits;
- update living README/roadmap/TRACE release labels from development-line wording to the named v0.3.3 baseline where appropriate;
- update `docs/release-versioning.md` to record v0.3.3 as the post-audit-v2 hardening/convergence patch;
- preserve `MODEL_SEMANTICS_ID = anthrosim-model-semantics-v21` unless release preparation itself changes authoritative scientific meaning (it should not);
- require all currently protected branch contexts on the exact candidate SHA;
- require exact-SHA M8.6, M9.7 and RustSec release-specific success;
- only then create/preserve immutable-intent tag `v0.3.3` on that exact verified current-main SHA.

## Audit conclusion

**Documentation readiness: small bounded cleanup required; no scientific-model documentation defect demonstrated.**

The repository has the necessary normative scientific, ODD/ODD+D, TRACE, audit, provenance and release-policy documentation to freeze a defensible `v0.3.3` baseline. The remaining work is current-status convergence in three living documents plus the intentional release-identity update itself.

After `v0.3.3` is frozen, a fresh independent/adversarial scientific audit is an appropriate convergence check before using AnthroSim for the first site inferential study. Per the audit protocol, repeated audits are evidence of process maturity rather than proof of correctness; a clean or progressively narrower fresh pass would be positive convergence evidence, while a new P0/P1 tranche would mean the framework is not yet ready to move on.
