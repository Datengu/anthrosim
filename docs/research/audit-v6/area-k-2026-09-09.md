# Scientific Audit v6 — Area K: experiment orchestration, configuration, provenance and reproducibility

Date: 2026-09-09

Immutable discovery target:

- release/tag: `v0.3.6`
- exact tag SHA: `7d5e47309556e458477cd7283230871363b2c89a`
- software version: `0.3.6`
- model semantics: `anthrosim-model-semantics-v35`

Area-K status: **complete — AV6-013 / #741 P1 open**.

No production repair was performed. AV6-013 remains deliberately unrepaired until Scientific Audit v6 discovery across Areas A–N is complete.

## Scope and overlap control

Area K started from zero fresh v6 coverage and independently challenged the orchestration/provenance boundary after the v0.3.6 repairs for prior study-finalization and binding defects.

Historical v4/v5 findings were used only as attack-design and duplicate-avoidance controls. In particular, this area did not replay v5's successful missing-`research-state.json` relocation/retry control.

The fresh Area-K program separated two verifier questions:

1. whether a finalized-study root verifier enforces the same canonical-analysis semantic contract as the authoritative `anthrosim-study finalize` producer;
2. whether a valid finalized binding can be transplanted between distinct frozen study/protocol roots whose research outputs are otherwise byte-identical.

## Fresh pass 1 — finalized-study producer/verifier semantic parity

Evidence-only PR: **#739**, closed unmerged.  
Exact controlled head: `86139b79459771a9dca6860907e6f3c9eec69627`  
Dedicated workflow: **34386105696**  
Dedicated job: **102582653156** — `Finalized-study producer/verifier semantic adversary`  
Central CI: **34386105450** — success  
Finding: **AV6-013 / #741 P1**

The adversary used a real exploratory study and real two-point research design with `resources.periodsPerYear = [4,12]`.

After untouched finalization and positive verification, it changed only the canonical point/run treatment and resulting-configuration rows from `[4,12]` to `[999,12]`, while preserving the immutable plan/state, child bundles, research ID, point/run IDs and source identity. It then recomputed both modified result-artifact digests and a fresh producer-defined `study-result-v1-*` identity, eliminating stale-hash/self-identity explanations.

Exact result:

```text
immutable_plan_treatments=[4, 12]
immutable_expected_treatments=[4, 12]
forged_points_treatments=[999, 12]
forged_runs_treatments=[999, 12]
forged_result_identity=study-result-v1-6ce1e03b16ffd143
producer_finalize_rejects=true
root_verifier_accepted=true
verified_result_identity=study-result-v1-6ce1e03b16ffd143
verified_research_id=research-execution-v1-699146192f3244ff
```

The producer rejected the forged canonical analysis artifact as differing from immutable research plan/state, confirming that the AV4-013 producer repair remains effective. The root-aware finalized-study verifier nevertheless accepted the freshly self-consistent reconstructed binding over those same scientifically false canonical bytes.

All ordinary/protected exact-head workflows passed; only the predeclared dedicated scientific oracle failed.

### Scientific interpretation

The root-aware verifier validates binding identity, digests and frozen study/protocol/root lineage, but it does not enforce the producer's full semantic validation/rederivation of canonical `research/analysis/points.json` and `runs.json` against immutable research plan/state.

A positive root-verification result can therefore authenticate canonical treatment/run tables that no authoritative research point executed, provided the post-finalization edit is coordinated with fresh digests and a fresh result identity.

This was preserved as **AV6-013 / #741 P1 — finalized-study root verifier accepts semantically forged canonical analysis rows**.

### Duplicate distinction

AV6-013 is distinct from:

- AV4-013/#543: the producer itself formerly accepted contradictory canonical analysis rows; v0.3.6 correctly rejects them;
- AV4-012/#539: stale `resultIdentity` acceptance; AV6-013 recomputes a fresh valid identity and artifact digests;
- AV6-012/#737: free-form deterministic identifiability output values not bound to executions;
- AV6-010/#729: confirmatory Monte Carlo sample values not semantically bound to study outputs.

## Fresh pass 2 — cross-root finalized-binding transplant control

Evidence-only PR: **#740**, closed unmerged.  
Exact controlled head: `9566eb98116a68618c95271c7c672c864ebd72e1`  
Dedicated workflow: **34386183485**  
Dedicated job: **102582914744** — `Cross-root finalized-study binding transplant control`  
Central CI: **34386183517** — success

Two real exploratory study roots were created with different frozen `studyId` / protocol identities but the exact same research definition/source. Their canonical research `points.json` and `runs.json` were byte-identical, isolating study-root/protocol lineage from research outputs.

Exact result:

```text
research_artifacts_byte_identical=true
untouched_a_verified=true
untouched_b_verified=true
transplanted_binding_rejected=true
accepted=false
error=study result binding field studyExecutionId does not match the frozen study plan/protocol
```

The root-aware verifier correctly rejected transplanting study B's otherwise valid binding into study A.

**Disposition: clean independent control / no new finding.**

This localizes AV6-013 to canonical-analysis semantic parity rather than a generic absence of study/protocol/root binding.

## Frozen-target orchestration/provenance assessment

The Area-K evidence confirms several important repaired controls on v0.3.6/v35:

- authoritative finalization rejects canonical point/run indexes contradicting immutable research plan/state;
- finalized binding self-identity and artifact digests alone are not the only producer-side protection;
- frozen study/protocol identity remains checked by root-aware verification;
- cross-root substitution of otherwise valid bindings fails closed;
- exact-head central CI and the separate scientific/security/provenance/determinism/observability/bundle/resume surfaces remained green under both attacks.

The remaining demonstrated gap is specifically verifier semantic parity with the authoritative producer.

## Completion assessment

Fresh Area-K evidence now includes:

1. a direct producer-versus-verifier semantic parity falsification through the real study/research/finalization chain (#739/#741 P1);
2. a causally independent cross-root binding-transplant control (#740, clean);
3. direct confirmation that prior producer-side canonical-analysis validation remains fail-closed on v0.3.6;
4. direct confirmation that study/protocol/root identity binding remains fail-closed;
5. exact-head separation of dedicated scientific assertions from ordinary protected/scientific workflows.

This is sufficient fresh falsification-oriented coverage for Area K under the v6 protocol.

**Area K is complete — non-clean through AV6-013 / #741 P1.**

No production semantics were changed during discovery.

## Handoff

Proceed to **Area L — observability, analysis outputs and statistical summaries** against immutable v0.3.6/v35.

Start from fresh v6 attacks. Prioritize:

- denominator and weighting semantics across runs, agents, households, cells and time;
- censoring, extinction, incomplete or operationally failed runs in derived summaries;
- time-window and time-aggregation boundaries;
- summaries that can hide multimodality or incompatible regimes;
- whether canonical/derived analysis can accidentally combine scientifically incompatible runs;
- nominal versus realized quantities and missingness semantics;
- whether uncertainty accompanies claim-driving estimates;
- whether AV6-007, AV6-010, AV6-012 or AV6-013 can propagate into analysis outputs without simply double-counting those findings.

AV6-013/#741 remains deferred until full A–N discovery is complete.
