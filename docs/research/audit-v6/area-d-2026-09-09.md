# Scientific Audit v6 — Area D completion report

Date: 2026-09-09  
Area: **D — resources, condition, subsistence, depletion/recovery**

Immutable audit target:

- release: `v0.3.6`
- tag/source SHA: `7d5e47309556e458477cd7283230871363b2c89a`
- software: `0.3.6`
- model semantics: `anthrosim-model-semantics-v35`

Area D began from zero after Areas A–C were dispositioned. Historical resource findings were inspected only for duplicate avoidance and attack design; they were not counted as v6 evidence. No production semantics were changed.

## Fresh evidence 1 — scarce-resource spatial-reflection equivariance (#705 → AV6-004/#707)

Evidence PR: **#705**, closed unmerged.  
Final controlled head: `f8ee562027ae162afb9ce7a5f00a6ca3382b0f33`.  
Central CI run: `34356842006`.  
`Quality and tests` job: `102483639065`.

The final fixture passed formatting and Clippy and all **284 pre-existing anthrosim-core tests** before the fresh oracle failed.

Construction:

- symmetric 2×1 world with base productivity `[1,1]`;
- two one-person households retain the same person IDs, household IDs, conditions and stochastic-coupling identities across arms;
- both households are colocated;
- canonical arm occupies `CellId(1)` and reflected arm occupies `CellId(2)`, the exact horizontal reflection;
- total need is two units and exactly one stock unit is available;
- condition recovery is zero and scarcity mortality disabled so the indivisible allocation recipient is visible directly in condition.

Positive controls in both arms were exact:

```text
total_need = 2
supplied = 1
unmet = 1
households_with_unmet_need = 1
```

Observed result:

```text
canonical=(1000, 0)
reflected=(0, 1000)
```

The same physical scarcity problem therefore gives the unit to the opposite abstract household solely because the occupied canonical cell index changes under reflection.

Mechanism: equal fractional remainders are ordered by the scientific household coupling key introduced after AV4-008/#514, but the tie award phase remains rotated by `(period_sequence + cell_index) % group_len`. The pure reflection changes `cell_index` while household coupling identities remain unchanged.

Mandatory duplicate search distinguished this from:

- #182, which addressed persistent first/lower-household remainder priority and introduced the period/cell rotation;
- AV4-008/#514, which demonstrated HouseholdId relabelling sensitivity and whose repair replaced canonical household ordering with scientific coupling keys while retaining the period/cell phase;
- prior M4/M9 spatial-reflection findings, which affect separate movement mechanisms.

No existing issue recorded this M3 resource-allocation spatial-reflection failure.

Disposition: **AV6-004 / #707 — P1, open**.

The severity is P1 because a pure representation-preserving spatial isomorphism immediately changes individual condition, which is causal for mortality, migration and subsequent resource/demographic trajectories.

## Fresh evidence 2 — M9 duration-split household relabelling (#706 → AV6-005/#708)

Evidence PR: **#706**, closed unmerged.  
Final controlled head: `ae667febd0930f43b50eae0af459c5fc8201f13f`.  
Central CI run: `34356930794`.  
`Quality and tests` job: `102483966686`.

The final head passed formatting and Clippy and all **284 pre-existing anthrosim-core tests** before the fresh oracle failed.

Construction isolates `duration_weighted_needs(...)`:

- one unit total household resource need;
- exactly 45 home-provisioning days and 45 visitor days;
- zero transit days;
- fixed visitor destination and resource-period sequence;
- exact 50/50 fractional claims;
- paired comparison changes only canonical household index `0` versus `1`.

Observed at period 0:

```text
canonical index 0 -> (home=1, visitor=0)
relabelled index 1 -> (home=0, visitor=1)
```

Demand conservation remains exact; pure canonical household relabelling moves the indivisible need remainder between physical home and visitor demand.

Mechanism: the exact-tie helper derives a balanced binary phase from `period_sequence` and raw `household_index`. This removes historical permanent home bias over repeated periods, but makes canonical household identity causal at a fixed scientific state.

Mandatory duplicate search distinguished this from:

- #194, which found the historical always-home 50/50 rounding bias and explicitly required that a repair not merely move bias into arbitrary household-ID ordering;
- repair #297, which introduced the current balanced household-index/period phase but did not establish fixed-period relabelling invariance;
- AV4-008/#514, which concerns the later competition among multiple claims inside a scarce cell rather than the earlier split of one household's need between home and visitor locations.

No existing issue recorded this exact fixed-period household-relabel failure.

Disposition: **AV6-005 / #708 — P2, open**.

The severity is P2 because the demonstrated discrepancy is one indivisible unit in an exact fractional tie and the current rule deliberately balances such errors across periods; nevertheless the physical demand location can change solely from bookkeeping identity and can alter visitor/home scarcity and downstream condition.

## Area-D conclusion

Status: **complete — non-clean via AV6-004/#707 P1 and AV6-005/#708 P2**.

Fresh Audit-v6 Area-D evidence independently attacked two different resource surfaces:

1. scarce-cell remainder allocation under a pure spatial reflection;
2. one-household M9 duration-weighted home/visitor attribution under pure household relabelling.

Both failures were controlled, passed the full pre-existing test suite first, survived mandatory duplicate review, and were preserved on closed-unmerged evidence PRs. No production repair was attempted.

Area-D completion does not establish empirical subsistence validity or calibrated resource realism. It records the fresh software/scientific-method disposition against immutable v0.3.6/v35.

## Next audit area

Proceed to **Area E — spatial landscape, movement, migration, temporary mobility and boundaries** from zero coverage. Reconstruct live repository/PR state before new evidence, use prior spatial/movement findings only as historical controls, and preserve AV6-004/AV6-005 unrepaired behind the A–N discovery barrier.
