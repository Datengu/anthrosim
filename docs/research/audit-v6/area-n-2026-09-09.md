# Scientific Audit v6 — Area N: cross-system integration

Date: 2026-09-09

Immutable discovery target:

- release/tag: `v0.3.6`
- exact tag SHA: `7d5e47309556e458477cd7283230871363b2c89a`
- model semantics: `anthrosim-model-semantics-v35`

Area-N disposition: **complete — non-clean via existing v6 findings; no new independent root finding demonstrated by the fresh integration attack**.

No production repair was performed during Area N. This report completes Audit-v6 discovery only after the evidence-only Area-N PR has fully green exact-head protected/scientific CI and is closed unmerged.

## Scope and anti-duplication control

Area N does not repeat every subsystem test. Its purpose is to challenge whether locally understood mechanisms compose into materially misleading scientific outcomes and to quantify cross-system consequences of already-preserved findings without opening duplicate issues for the same smallest cause.

At Area-N entry, protected `main` was `d341d0a6462f175bcfd1f26df148d074e9f80f4a` after Area M completion. Audit-v6 Areas A–M were complete with 14 open findings: 7 P1 and 7 P2. Production remediation remained prohibited until Area N completion.

The fresh Area-N program deliberately targeted a composition that had not been established by the original Area-E evidence: **AV6-006 M9 spatial-domain locality × temporary aggregation × M3 duration-aware resource accounting**.

## Fresh integration adversary — irrelevant M9 padding propagates into aggregation and resources

Evidence-only PR: **#749**  
Final controlled head: `e0b636735c486e0318004cb9cbe558de50d02ced`  
Dedicated workflow: `34409746294`  
Dedicated job: `M9 padding propagation into aggregation/resources`

The final scientific construction is intentionally derived from, but materially extends, AV6-006/#711.

### Controlled local problem

Baseline arm:

- 3×1 world;
- one declared household resident at Cell 2;
- focal region `{Cell 1, Cell 3}`;
- Cells 1–3 all have model-facing movement cost exactly 1,000 and base productivity exactly 1,000;
- one M9 departure-day trigger at day 20;
- five-day visit;
- M4 disabled;
- mortality and fertility disabled;
- one annual M3 resource period;
- one unit of need per person-day;
- zero seasonality, so the controlled fixture has no realized regeneration while preserving positive initial/capacity stock.

Padded arm:

- Cells 1–3 are unchanged;
- one Cell 4 is appended outside the focal region;
- Cell 4 movement cost is exactly 6,000;
- M9 traversal ceiling is 5,000, so Cell 4 is explicitly impassable;
- the padding cell therefore cannot enter either local equal-cost route, candidate set or visitor destination.

Environment/population realization seeds are fixed between arms. Process seed is varied only to locate the first full-host reproduction of AV6-006's equal-cost destination flip.

### Exact integrated result

At process seed 0 the full authoritative spatial host produced:

```text
baseline_destination=CellId(1)
padded_destination=CellId(3)
baseline_visitor_person_days=[5, 0, 0]
padded_visitor_person_days=[0, 0, 5]
baseline_food_stock=[9995, 9640, 10000]
padded_food_stock=[10000, 9640, 9995]
```

Thus a causally irrelevant unreachable padding cell changes not only the M9 destination identity but the downstream scientific state:

- five actual visitor-person-days move from unchanged Cell 1 to unchanged Cell 3;
- the residence Cell 2 resource stock is identical in both arms (`9640`);
- the previously visited Cell 1 retains exactly five additional units under padding (`9995 -> 10000`);
- the newly visited Cell 3 loses exactly five units (`10000 -> 9995`).

The one-person fixture makes the resource consequence hand-computable: five visiting days create exactly five units of destination-attributed M3 demand.

### Evidence hygiene

Two earlier branch heads are explicitly excluded from scientific evidence:

1. the first attempt failed compilation because the disposable audit fixture used one obsolete `ResourceConfig` field name;
2. the second attempt accidentally set regeneration-per-productivity to zero, which also collapsed resource capacity to zero and therefore could not test depletion.

Only fixture plumbing was corrected. The final local-equivalence, destination-flip, visitor-person-day and five-unit resource assertions were not weakened.

### Interpretation

This is **not a new AV6 root finding**. The smallest cause remains AV6-006/#711: the M9 equal-cost ambiguity is nonlocal because whole-world canonicalization lets unreachable impassable padding relabel unchanged local alternatives.

Area N establishes the material coupled consequence that was only prospective in Area E: the same nonlocal representation leak changes temporary concentration and resource extraction in the unchanged local physical problem. This confirms why AV6-006 is cross-cutting through F/D/N and why a future repair must be tested beyond event identity alone.

## Cross-system synthesis of v6 findings

The full v6 finding set remains 14 findings; no fifteenth issue is created merely because several findings have downstream consequences.

- **AV6-001/#687 — scheduler × movement × aggregation/resources.** A newly due target-arrival M9 departure can occur after same-day M4, violating the declared causal order. This changes the residence from which the journey is created and can alter later presence/resource/aggregation state.
- **AV6-002/#694 — demography × initialization/kinship.** Male-parent age eligibility uses inconsistent time references for founder versus dynamic candidates, so parentage/kin structure can depend on provenance category rather than one biological-time contract.
- **AV6-003/#699 — households/kin × movement.** External-kin context can collapse before a final PersonId tie-break, allowing arbitrary representation to alter household topology and thereby later mobility/resource-sharing structure.
- **AV6-004/#707 and AV6-005/#708 — resources × spatial/household representation.** Scarce-resource outcomes can change under reflection and duration splitting can depend on household index, with consequences for condition, mortality and movement pressure.
- **AV6-006/#711 — spatial M9 × aggregation × resources.** Fresh Area-N evidence quantifies the propagation directly: irrelevant padding flips five visitor-person-days and five resource units between unchanged local cells.
- **AV6-007/#718 — aggregation × observability.** Touching half-open visits can inflate `peakVisitors`, so interaction/crowding interpretation can disagree with positive-duration overlap.
- **AV6-008/#721 — initialization × household lifecycle.** Epistemically incomplete founder genealogy is admitted into dependency-aware fission where missing relationships become structural inputs.
- **AV6-009/#726 and AV6-010/#729 — stochastic inference × downstream research claims.** Sequential confidence semantics under-cover, and confirmatory per-seed values can be provenance-valid yet contradict authoritative runs. Any robustness/calibration layer consuming those results inherits the limitation.
- **AV6-011/#733 — sensitivity × long-run claims.** No-op horizon/window declarations can satisfy mandatory sensitivity coverage, weakening equilibrium-like claim support.
- **AV6-012/#737 — executed design × calibration/identifiability.** Claim-driving deterministic outputs are not semantically bound to authoritative executions, so correct design coordinates alone do not establish valid inference.
- **AV6-013/#741 — orchestration/provenance × downstream verification.** Finalized-study root verification can accept freshly re-identified forged canonical rows even when the producer finalizer rejects them.
- **AV6-014/#745 — initialization sensitivity × observability/interpretation.** Materially distinct drifting trajectories can collapse to identical `status:drifting` outcome distributions and hide path dependence.

These interactions establish that the non-clean v6 result is not a collection of isolated presentation defects. Several findings reach neighbouring causal or inference layers, and the new Area-N executable control demonstrates one such propagation quantitatively end-to-end.

## Area-N completion assessment

Area N now has genuinely fresh coupled evidence rather than only a list of cross-cutting labels. The M9 padding attack composes spatial coupling, temporary physical presence, aggregation observability and M3 resource accounting in one authoritative execution.

No new smallest independent defect was demonstrated beyond AV6-006 in this pass, so no duplicate issue is opened.

**Area N is complete — non-clean via the existing Audit-v6 finding set, with no additional AV6 finding from the fresh integration adversary.**

## Audit-v6 discovery conclusion

With Area N complete, Audit-v6 discovery covers **14/14 Areas A–N** against immutable `v0.3.6` / model semantics v35.

Discovery is a **non-clean convergence pass** with 14 preserved findings:

- **P1 (7):** AV6-001/#687, AV6-004/#707, AV6-006/#711, AV6-009/#726, AV6-010/#729, AV6-012/#737, AV6-013/#741;
- **P2 (7):** AV6-002/#694, AV6-003/#699, AV6-005/#708, AV6-007/#718, AV6-008/#721, AV6-011/#733, AV6-014/#745;
- **P0/P3:** none.

Because v6 discovered new P1 defects, it cannot satisfy the charter's P1-clean convergence objective even after these findings are repaired. The next phase is controlled remediation/disposition of the 14 findings. Every P0/P1 repair requires exact-head production validation and independent adversarial re-verification under the reusable protocol. After remediation, the repaired line must be frozen as a new immutable release and another fresh audit generation is required before the framework-convergence gate for empirical work can be considered satisfied.

This discovery completion does **not** imply empirical or archaeological validity or readiness for a site-specific study.
