# AnthroSim v0.3.4 — post-Scientific-Audit-v3 convergence baseline

Release date: 2026-09-02  
Software version: `0.3.4`  
Model semantics: `anthrosim-model-semantics-v25`  
Previous release: `v0.3.3` / `anthrosim-model-semantics-v21`

## Purpose

v0.3.4 freezes the complete Scientific Audit v3 remediation line as a named patch baseline. It does not add a new roadmap milestone and the release-preparation change itself does not alter causal simulator behaviour.

Scientific Audit v3 interrogated the `v0.3.3` / model-semantics-v21 release baseline across Areas A–N. Following the explicitly authorized repository-wide privacy sanitisation on 2026-09-02, the live rewritten `v0.3.3` tag resolves to `d3b6fc0b0750933b57252c9087513d156d88f218`; that Git-identity rewrite did not change the released version or v21 scientific semantics. Discovery demonstrated 17 findings: **1 P0, 6 P1 and 10 P2**. Post-discovery remediation repaired all 17 through dedicated protected production PRs and independently re-ran each original adversary against merged `main` before closure. At release preparation there are **0 open original Audit-v3 findings**.

## Scientific semantics progression since v0.3.3

Four Audit-v3 repairs changed authoritative causal model behaviour and therefore advanced model semantics:

- v21 → v22: spatial/core parity for declared founder reproductive history;
- v22 → v23: declared founder reproductive chronology is reconciled with explicitly represented children;
- v23 → v24: zero-recovery full-supply M3 processing preserves latent fixed-point condition deterioration;
- v24 → v25: dependency-aware household fission becomes relationship-aware before arbitrary PersonId tie-breaking.

Other Audit-v3 repairs affected analysis, provenance, research orchestration, statistical inference, reporting or documentation and correctly did **not** manufacture causal-semantics bumps.

## Major scientific/research-integrity hardening

The v0.3.4 line additionally includes, among other repairs:

- fail-closed exact research source identity rather than accepting `gitCommit=null` as a unique executable identity;
- coherent independent-arm Monte Carlo seed design and covariance-aware paired inference;
- fail-closed execution binding for support-sensitivity evidence;
- protection against overlapping research dimensions silently erasing treatments;
- type-preserving structural identifiers in identifiability analysis;
- conservative held-out prediction envelopes;
- rejection of false identification when a claimed parameter was not varied;
- explicit nuisance-parameter equifinality reporting;
- stable evidence/source identity binding for held-out corroboration;
- current v2 household-effect summaries with complete 3 × 130 = 390 same-seed paired contrasts;
- synchronized current demographic narrative/TRACE evidence and household-lifecycle model identity.

## Demographic confirmatory revalidation

Causal semantics changes were followed by fresh protected demographic confirmation rather than silently carrying prior scientific outputs forward. The final v25 general-demography confirmation uses 3 demography schedules × 2 household-lifecycle arms × 130 seeds = **780 runs** and **390 same-seed paired household contrasts**. The high-level recommendation remains `no_universal_demographic_baseline` under the declared synthetic experiment; this is a model-form result, not empirical prehistoric calibration.

## Interpretation limits

v0.3.4 is a stronger **framework verification/convergence** baseline. It does **not** establish that AnthroSim is empirically calibrated for a real archaeological population, validate a specific archaeological site or another site, prove model correctness, or make every future inference TRACE-complete. Question-specific evidence quality, uncertainty, calibration/validation, applicability and genuinely held-out corroboration remain separate requirements.

## Release gate

The `v0.3.4` tag must be created only on the exact current protected-`main` release-candidate SHA after:

1. package version, `CITATION.cff`, this release note and living release documentation all identify v0.3.4/v25 consistently;
2. every currently required protected-main status context is successful on that exact SHA;
3. exact-SHA release-specific M8.6, M9.7 and RustSec dispositions are successful;
4. there are no open release-preparation PRs or demonstrated release-blocking defects.

The `v0.3.3` release/version-semantics identity and Audit-v3 scientific evidence remain historical provenance. Their Git object identities and private textual references were privacy-sanitised on 2026-09-02; this v0.3.4 release does not reinterpret or revalidate those historical scientific conclusions merely because their Git identities changed.
